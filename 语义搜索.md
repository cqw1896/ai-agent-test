根据搜索结果，LangChain 提供了 `init_embeddings` 函数来初始化嵌入模型，类似于 `init_chat_model` 用于聊天模型 [[1]]。以下是使用第三方模型（如 OpenAI）的完整代码：

```python
# 语义搜索 - 使用第三方模型（OpenAI）
## 索引
# 1. 读取PDF，按照页来管理，每页封装成一个Document对象, List[Document]
# 2. 分割文本, 文本段（chunk），每段封装成一个Document, List[Document]
# 3. 向量化：  文本段 <=> 向量， 需要嵌入模型来辅助
# 4. 向量库：  把多个 文本段:向量 存到向量库，OK了。
# uv add pypdf langchain-openai

import os
import sys

# 【关键】清除所有代理环境变量，强制直连
for key in ['http_proxy', 'https_proxy', 'HTTP_PROXY', 'HTTPS_PROXY', 'all_proxy', 'ALL_PROXY']:
    if key in os.environ:
        del os.environ[key]

# 设置 OpenAI API Key（请替换为你的实际密钥）
os.environ['OPENAI_API_KEY'] = 'your-openai-api-key-here'

# 1. 读取PDF，按照页来管理，Document, List[Document]
from langchain_community.document_loaders import PyPDFLoader

file_path = 'Git.pdf'
loader = PyPDFLoader(file_path)
docs = loader.load()
print(len(docs))  # 117
print(type(docs[0]))  # <class 'langchain_core.documents.base.Document'>
print(docs[0])  

# 2. 分割文本，文本段(chunk), Document, List[Document]
from langchain_text_splitters import RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200,
    add_start_index=True
)

all_splits = text_splitter.split_documents(docs)  # List[Document]
print('-' * 80)
print(len(all_splits))  # 134
print(all_splits[0])

# 3. 向量化：文本段 <=> 向量，需要嵌入模型来辅助
# 4. 文本块:向量 的存储, 包含第3步
from langchain_chroma import Chroma
from langchain.embeddings.base import init_embeddings

# 使用 init_embeddings 初始化 OpenAI 嵌入模型
# 等价于 from langchain_openai import OpenAIEmbeddings
embedding = init_embeddings('openai:text-embedding-3-small')

vector_store = Chroma(
    collection_name='example_collection',
    embedding_function=embedding,
    persist_directory='./chroma_langchain_db_openai'
)

ids = vector_store.add_documents(documents=all_splits)
print('*' * 80)
print(len(ids))
print(ids)
```

**主要改动说明：**

1. **导入变更**：将 `from langchain_ollama import OllamaEmbeddings` 替换为 `from langchain.embeddings.base import init_embeddings` [[2]]

2. **模型初始化**：将 `OllamaEmbeddings(model='nomic-embed-text')` 替换为 `init_embeddings('openai:text-embedding-3-small')`，使用 OpenAI 的嵌入模型

3. **API Key 配置**：需要设置 `OPENAI_API_KEY` 环境变量

4. **持久化目录**：更改为 `./chroma_langchain_db_openai` 以区分不同模型的向量库

**其他可选的第三方模型：**
- `init_embeddings('openai:text-embedding-ada-002')` - OpenAI 旧版模型
- `init_embeddings('cohere:embed-english-v3.0')` - Cohere 模型
- `init_embeddings('google-genai:models/text-embedding-004')` - Google 模型

使用前需安装对应依赖包，如 `uv add langchain-openai` [[12]]。

阿里云百炼（DashScope）的嵌入模型可以通过 `langchain_community` 中的 `DashScopeEmbeddings` 类来使用。虽然 `init_chat_model` 主要用于聊天模型，但嵌入模型可以使用 `init_embeddings`（如果支持）或直接使用 `DashScopeEmbeddings`。

以下是使用阿里云百炼嵌入模型（如 `text-embedding-v3`）的代码：

```python
# 语义搜索 - 使用阿里云百炼（DashScope）嵌入模型
## 索引
# 1. 读取PDF，按照页来管理，每页封装成一个Document对象, List[Document]
# 2. 分割文本, 文本段（chunk），每段封装成一个Document, List[Document]
# 3. 向量化：  文本段 <=> 向量， 需要嵌入模型来辅助
# 4. 向量库：  把多个 文本段:向量 存到向量库，OK了。
# uv add pypdf langchain-community dashscope

import os
import sys

# 【关键】清除所有代理环境变量，强制直连
for key in ['http_proxy', 'https_proxy', 'HTTP_PROXY', 'HTTPS_PROXY', 'all_proxy', 'ALL_PROXY']:
    if key in os.environ:
        del os.environ[key]

# 设置阿里云百炼 DashScope API Key（请替换为你的实际密钥）
# 也可以在创建 DashScopeEmbeddings 时直接传入 api_key 参数
os.environ['DASHSCOPE_API_KEY'] = 'your-dashscope-api-key-here'

# 1. 读取PDF，按照页来管理，Document, List[Document]
from langchain_community.document_loaders import PyPDFLoader

file_path = 'Git.pdf'
loader = PyPDFLoader(file_path)
docs = loader.load()
print(len(docs))  # 117
print(type(docs[0]))  # <class 'langchain_core.documents.base.Document'>
print(docs[0])  

# 2. 分割文本，文本段(chunk), Document, List[Document]
from langchain_text_splitters import RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200,
    add_start_index=True
)

all_splits = text_splitter.split_documents(docs)  # List[Document]
print('-' * 80)
print(len(all_splits))  # 134
print(all_splits[0])

# 3. 向量化：文本段 <=> 向量，需要嵌入模型来辅助
# 4. 文本块:向量 的存储, 包含第3步
from langchain_chroma import Chroma
from langchain_community.embeddings import DashScopeEmbeddings

# 使用 DashScopeEmbeddings 初始化阿里云百炼嵌入模型
# 常用模型: text-embedding-v3, text-embedding-v2, text-embedding-v1
embedding = DashScopeEmbeddings(
    model='text-embedding-v3',
    dashscope_api_key=os.environ.get('DASHSCOPE_API_KEY')
)

vector_store = Chroma(
    collection_name='example_collection',
    embedding_function=embedding,
    persist_directory='./chroma_langchain_db_dashscope'
)

ids = vector_store.add_documents(documents=all_splits)
print('*' * 80)
print(len(ids))
print(ids)
```

**主要改动说明：**

1. **导入变更**：将 `from langchain_ollama import OllamaEmbeddings` 替换为 `from langchain_community.embeddings import DashScopeEmbeddings` [[24]]

2. **模型初始化**：使用 `DashScopeEmbeddings(model='text-embedding-v3')` 初始化阿里云百炼的嵌入模型 [[32]]

3. **API Key 配置**：设置 `DASHSCOPE_API_KEY` 环境变量，或在构造函数中直接传入 `dashscope_api_key` 参数 [[24]]

4. **持久化目录**：更改为 `./chroma_langchain_db_dashscope` 以区分不同模型的向量库

**关于 `init_chat_model`：**

`init_chat_model` 是用于初始化**聊天模型**（LLM）的函数，不是用于嵌入模型 [[34]]。如果你想同时使用阿里云百炼的聊天模型（如 Qwen）和嵌入模型，可以这样写：

```python
from langchain.chat_models import init_chat_model
from langchain_community.embeddings import DashScopeEmbeddings

# 聊天模型（使用 init_chat_model）
llm = init_chat_model(
    model="qwen-plus",
    model_provider="openai",  # 百炼兼容 OpenAI 协议
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
    api_key=os.environ.get('DASHSCOPE_API_KEY')
)

# 嵌入模型（使用 DashScopeEmbeddings）
embedding = DashScopeEmbeddings(
    model='text-embedding-v3',
    dashscope_api_key=os.environ.get('DASHSCOPE_API_KEY')
)
```

**依赖安装：**
```bash
pip install langchain-community dashscope
# 或使用 uv
uv add langchain-community dashscope
```

### 1. 代码改动部分

只需修改文件头部，使用 `python-dotenv` 加载 `.env` 文件：

```python
# 替换原来的第 12-22 行
import os
from dotenv import load_dotenv

# 加载同目录下的 .env 文件
load_dotenv()

# 【关键】清除所有代理环境变量，强制直连
for key in ['http_proxy', 'https_proxy', 'HTTP_PROXY', 'HTTPS_PROXY', 'all_proxy', 'ALL_PROXY']:
    if key in os.environ:
        del os.environ[key]

# 不再需要硬编码 API Key，直接从环境变量获取
# os.environ['DASHSCOPE_API_KEY'] = 'your-dashscope-api-key-here'  # 删除这行
```

**说明：**
- `load_dotenv()` 会自动查找并加载当前目录下的 `.env` 文件
- 加载后，`.env` 中的变量会注入到 `os.environ` 中
- 后续代码 `os.environ.get('DASHSCOPE_API_KEY')` 无需修改，直接使用即可

**需要安装的依赖：**
```bash
uv add python-dotenv
```

---

### 2. `.env` 文件中存放多个 API Key

**.env 中完全可以放多个 API Key**，变量命名通常遵循**大写 + 下划线**的约定，格式为：`{服务名}_API_KEY` 或 `{服务名}_API_KEY`。

**示例 `.env` 文件：**
```bash
# 阿里云百炼
DASHSCOPE_API_KEY=sk-your-dashscope-key

# OpenAI
OPENAI_API_KEY=sk-your-openai-key

# Anthropic
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google AI
GOOGLE_API_KEY=your-google-key

# 自定义前缀（推荐用项目/服务名区分）
MYAPP_DASHSCOPE_KEY=sk-...
MYAPP_OPENAI_KEY=sk-...
```

**在代码中调用：**
```python
from dotenv import load_dotenv
import os

load_dotenv()

# 获取不同服务的 API Key
dashscope_key = os.environ.get('DASHSCOPE_API_KEY')
openai_key = os.environ.get('OPENAI_API_KEY')
anthropic_key = os.environ.get('ANTHROPIC_API_KEY')

# 使用
embedding = DashScopeEmbeddings(
    model='text-embedding-v3',
    dashscope_api_key=dashscope_key
)
```

**命名建议：**
- 使用**大写**和**下划线**（如 `DASHSCOPE_API_KEY`）
- 用服务名作为前缀（如 `OPENAI_`、`ANTHROPIC_`）
- 避免使用特殊字符